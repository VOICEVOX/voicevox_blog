---
interface Props {
  class?: string;
  isDark?: boolean; // Nemoやソングのページかどうか
}

const { class: className, isDark } = Astro.props;
---

<section class="py-12">
  <div class={`${className} max-w-5xl mx-auto px-4 flex flex-col`}>
    <h2
      id="engine"
      class="pt-[calc(3.25rem+1rem)] -mt-[calc(3.25rem+1rem)] text-3xl font-bold mb-6"
    >
      <a
        href="#engine"
        class={isDark
          ? "text-white dark:text-white"
          : "text-neutral-950 dark:text-neutral-950"}
      >
        音声合成エンジン
      </a>
    </h2>
    <p class="text-xl">
      音声合成エンジンを使えば、他のソフトウェアやアプリケーションから VOICEVOX
      音声を生成できます。
    </p>
    <p class="text-xl">
      Windows / macOS / Linux で動作する実行ファイルのほか、Docker
      イメージとしても配布しています。
    </p>
    <p class="text-xl">
      詳しくは&nbsp;<a
        href="https://github.com/VOICEVOX/voicevox_engine"
        class="font-bold underline text-[rgb(48,100,57)] hover:text-[rgb(38,80,47)] dark:text-[rgb(165,212,173)] dark:hover:text-[rgb(185,232,193)]"
        target="_blank"
        rel="noreferrer">VOICEVOX ENGINE</a
      >&nbsp;をご参照ください。
    </p>
  </div>
</section>
