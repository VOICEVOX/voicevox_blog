---
/**
 * 記事ディレクトリにある記事一覧を表示するページ。
 * ファイル名の逆順に表示する。
 */

import { withBaseUrl } from "@/helper";
import Base from "@/layouts/Base.astro";
import shareThumb from "./_share.jpg";
import { articles } from "./constants";

// TODO: リリース時にnoindexを外す
---

<Base
  title="ニュース | VOICEVOX"
  description="無料で使えるテキスト読み上げ・歌声合成ソフトウェア、VOICEVOXのニュース一覧です。"
  image={shareThumb}
  imageAlt=""
  noindex={true}
>
  <main class="py-12">
    <div class="max-w-[960px] mx-auto px-4">
      <h1 class="text-3xl font-bold text-gray-950 mb-6">ニュース</h1>
      {
        articles.map((article) => (
          <div class="mb-3">
            <a
              class="text-[rgb(48,100,57)] hover:text-[rgb(38,80,47)] underline"
              href={withBaseUrl(`/news/${article.frontmatter.slug}`)}
            >
              {article.frontmatter.title}
            </a>
            <p class="text-gray-400">{article.frontmatter.date}</p>
          </div>
        ))
      }
    </div>
  </main>
</Base>
