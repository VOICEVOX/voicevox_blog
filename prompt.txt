このリポジトリは bulma フレームワークに依存しています。
その依存をなくし、tailwindcssとzag.jsを使用するように置き換えることが最終目標です。
zag.jsはReact版の他にAstroのアイランドを介さないバニラでも利用します。

タスクを順に進めていきましょう。

git add・git commit・git reset・git restoreは絶対に勝手に行わないでください。

コードを書き足す際は周囲にあるコードや兄弟ファイルのコードをよく観察して、フォーマット、書き方、処理順序、ファイル名など、なるべく既存のコードと同じスタイルで書いてください。

実行チェックには開発サーバー`http://127.0.0.1:4321`へアクセスへplaywright-browser-operatorサブエージェントを使ってplaywright mcpを使った動作確認をしてください。
初回アクセス時は時間がかかることがありますが、ホットリロードが効くので、２回目以降の確認は速いはずです。

もしスクリーンショットを更新する際は、`pnpm run test-build`を実行し、`STRICT_USE_PREVIEW=1 pnpm run test:e2e 対象ファイルパス --update-snapshots`を実行してください。
`test-build`を忘れるとスクリーンショットが正しく更新されません。
変化しないはずのスクリーンショットが変わってしまった場合は、原因を特定して修正してください。

置き換えるときは既存のコンポーネントなどを直接Tailwind等で置き換えてください。
一時的に本体に問題があるかもしれませんが、その状態で進めてください。

以前のBulmaのときのスクリーンショットもいろいろと見てください。
でもあなたは結構画像認識の精度が良くなく、スペースの幅や色をよく間違うので、computedで情報を得るのが確実です。
目的はデザインを大きく変えずにtailwind化することなのでそこは注意してください。
Codexの場合はplanモードが無いので、最初に変更計画を練ったあと、ユーザーにその方針で良いか**必ず**訪ねてください。

primary color以外はtailwindにある色を使うようにしてください。

スクリーンショットを更新する際は、更新前にまずplaywrightで意図通りに実装できているか確認したあと、ユーザーに対して開発サーバーで確認を行うようにaskskillで質問してください。

Codexの場合、e2eテストには権限の昇格が必要になることがあります。
開発サーバーは起動しているので、**絶対に疎通チェックは一切せず**いきなりplaywrightで動作確認してください。
サーバーの起動は絶対に行わないでください。

---

tailwindを導入します。
astro環境でtailwiandを導入する方法をcontext7で調べて実行してください。

zagjsを導入してください。
やり方はcontext7で調べてください。

少しずつUIを移行していくために、UIの確認用のページを作って、Bulmaを用いた状態でスクリーンショットテストを追加してください。
`src/pages/dev/thumb_generator`と同じように`src/pages/dev/ui`ディレクトリを作成してください。
まずはBulmaを用いてスクリーンショットを作成しますが、後々はtailwindに変えていくので、その機構も考慮して実装してください。

追加したページはSEO対策に注意し、インデックスを阻止してください。
`src/pages/dev/thumb_generator`以下のページを参考にしてください。

Modal確認用のページを作成してください。
対象のModalは`src/components/modal/Modals.astro`にあるものすべてです。
最初に一番簡単そうなModal１つ選んで、importして表示するだけのページを作成してください。
ボタンを押したら開くのではなく、ページにアクセスしたら最初からModalが開いている状態にしてください。
Modalを開くには`src/store/index.ts`のコードを正しく理解する必要があるので読んでください。
それで撮ったスクリーンショットが意図したものになっているか確認してください。
意図したものになっていたら、他のモーダルも同様に追加してください。

Modalの`dev/ui`ページはtsxラッパーを作るのではなく、既存のコンポーネントをimportして実装してください。
storeをうまく使いつつ、astroの仕組みも把握する必要があると思います。
課題点を洗い出したらcontext7で方法を調べてください。
このタスクは思っている以上に難しいので、過信せず、１つ試しに実装して開発サーバーを起動してサブエージェントで動作確認してください。
１つ動いたら大丈夫なので、そのあと他のModalも実装してスクリーンショットを更新してください。

Downloadモーダルをtailwindcssを使って置き換えてください。
そこそこ近い見た目であれば大丈夫です。
おそらくzagjsはまだ不要だと思います。
今回のタスクは曖昧なので、曖昧な点がなくなるまでユーザーにaskskillで質問してください。

他のModalも同様にtailwindcssを使って置き換えてください。
primaryとemelaldは違うので元がどっちだったかを意識してください。

`dev-ui-modal.spec.ts`を`dev-ui/modal.spec.ts`に移動してください。
今後は`dev/ui`ディレクトリにUI確認用のテストをまとめてください。
それと`src/pages/dev/ui`にindex.astroを作成して、`dev/ui`以下のコンポーネントへ遷移するボタンを配置してください。
今後追加するUI確認用ページはすべてここからアクセスできるようにしてください。

`dev/ui`に足していけそうなUIコンポーネントを探して列挙してください。
簡単順に並べ、どこからどこへ移す何なのかが端的にわかるリストにしてください。
`todo-component-list.md`に書き出してください。

`todo-component-list.md`を上から順に１つずつ`dev/ui`に追加していってください。
createElementなどは使わず、既存コンポーネントをimportして表示してください。
もしコンポーネント切り出しをする必要がある場合は、方法を考えてユーザーへAskSkillで実行するかどうかを質問してください。

まずはbulmaを用いたページを作り、開発環境でplaywrightで確認して意図通りか確かめ、それが実際に使われているページの見た目と一致しているか確認した後、テストを更新して、スクリーンショットを撮りましょう。
スクショ更新コマンドはこちらです。
`STRICT_USE_PREVIEW=1 pnpm run test:e2e 対象ファイルパス--update-snapshots=all tests/e2e/ui/dev-ui/`

必要なimportは`import "@/styles/bulma.scss"`と`import "@/styles/global.scss"`になると思います。
インデックスを阻止することを忘れないでください。

大体のコンポーネントの切り出しができたので、次はtailwindcssを使って置き換えていきます。
まずは`todo-component-list.md`のタスクリストを、tailwind化のために書き換えてください。

`todo-component-list.md`のTailwind化タスクを上から順に１つずつ進めてください。
手順：Planモードで→Editモード→既存コンポーネントをTailwindで置き換え→開発サーバーでplaywright mcpやsubagentで確認→ユーザー確認→スクリーンショット更新

確認は `/dev/ui` から行ってください。
そこそこ近い見た目であれば大丈夫ですが、大きくハズレていたら修正してください。
既存のスクリーンショットと見比べたり、bulma時代(commit hash:3b5a86938c5ad5c0dbe6720ed6f6040aad840e19)のページと見比べたりしてください。
Bulma時代の例は`http://localhost:54321/dev/ui/`で見ることができます。
Playwrightのcomputedで確かめたりして近いtailwindのクラスを探してください。
起動していなければユーザーに報告してください。

.astroファイルをTailwind化する際、cssのimportを変える指示を忘れることがよくあるので注意してください。
`import "@/styles/global.css";`を忘れずに行ってください。
`bulma.scss`等のimportを消すのも忘れがちなので注意してください。

まずはBase.astroだけが書いた`dev/ui/base/`ページをBulmaベースで作ってください。
tailwind化は今回のタスク範囲外です。
完成したら待機してください。

`todo-page-list.md`のTailwind化タスクを上から順に１つずつ進めてください。
手順：既存ページをTailwindで置き換え→開発サーバーでplaywright mcpやsubagentで新旧を確認→ユーザー確認→スクリーンショット更新

確認は `http://localhost:4321/` から行ってください。
そこそこ近い見た目であれば大丈夫ですが、大きくハズレていたら修正してください。
既存のスクリーンショットと見比べたり、Playwrightでcomputedな値を取得して比較したり、bulma時代(commit hash:3b5a86938c5ad5c0dbe6720ed6f6040aad840e19)のページと見比べたりしてください。
Bulma時代の例は`http://localhost:54321/`で見ることができるので、間違えそうなものはcomputedで測って比較してください。
Playwrightのcomputedで確かめたりして近いtailwindのクラスを探してください。
サーバーが起動していなければユーザーに報告してください。

実装する際、すでにTailwind化が完了しているコンポーネントやページを積極的に再利用・参考にしてください。
bulmaとtailwindの対応をすでに取った`bulma-tailwind-map.md`ファイルも必ず読んでください。
bulmaのスタイルとtailwindのクラスは完全一致させる必要はないことに留意してください。
`src/layouts/Base.astro`でimportしているものはimport不要なので気をつけてください。

.astroファイルをTailwind化する際、cssのimportを変える指示を忘れることがよくあるので注意してください。
`bulma.scss`等のimportを消すのも忘れがちなので注意してください。

文字色や背景色などをBulma時代から勝手に微妙に変更しないでください。
そちらのほうが良いと思ってなどと勝手に変更するのは完全に不要です。気をつけてください。
ページごとにダークモード・ライトモードは固定なので、もう片方が不要な場合はもう片方を実装しないでください。

大体のページはレスポンシブデザインになっています。
確認の際、400px・800px・1300px幅で確認してください。

既存のtailwind classを使うようにし、`text-[32px]`や`m-[13px]`のような細かい指定はやめてください。
どうしても導入が必要だった場合はユーザーにaskskillで相談してください。

おそらくzagjsはまだ不要だと思います。
今回のタスクは曖昧なので、曖昧な点がなくなるまでユーザーにaskskillで質問してください。

完了後はチェックボックスを埋めてください。
１つやったらタスク完了です。

margin、padding、gap系をspacing値に置き換えてください。
まずtailwindの`@utility`に関して良く調べてください。
続いて以下の対応に従ってpadding・margin・gapのutilityを作ってください。
- 2xs: `--spacing * 1`
- xs: `--spacing * 2`
- sm: `--spacing * 3`
- md: `--spacing * 4`
- lg: `--spacing * 5`
- xl: `--spacing * 6`
- 2xl: `--spacing * 8`
- 3xl: `--spacing * 12`
- 4xl: `--spacing * 20`
定義は`src/styles/global.css`に書きましょう。

margin、padding、gap、leading系を先ほどのmdやlgなどに置き換えてください。
これらに当てはまらないもの、例えば`m-7`や`gap-[1.5px]`などのものはそのままにしてください。
sedで置き換えるのが良さそうです。`py-1.5`など、間違ったものが巻き込まれないように十分気をつけてください。

確認は `http://localhost:4321/` から行ってください。
そこそこ近い見た目であれば大丈夫です。
Playwrightでcomputedな値を取得して、bulma時代(commit hash:3b5a86938c5ad5c0dbe6720ed6f6040aad840e19)のページと見比べたりしてください。
Bulma時代の例は`http://localhost:54321/`で見ることができるので、間違えそうなものはcomputedで測って比較してください。
Playwrightのcomputedで確かめたりして近いtailwindのクラスを探してください。
サーバーが起動していなければユーザーに報告してください。

PlayButtonのローディング状態に関して、アクセシビリティを少し気にしてください。
ローディング中だということが分かるように属性を付与してください。
過剰な属性付与は返って混乱になるので、過不足ない形を考えて提案してください。

全ページの中で作られているボタンをかき集めて、どこにどういうボタンがあるのかリストアップして教えて下さい。
ほとんど同じものはまとめてください。
aタグのものとbuttonタグのものがあるので両方とも探してください。
Exproleで探すのではなく、grep等で探した方が精度が高いと思います。
見た目がほぼ同じであればaタグとbuttonタグが混在していてもまとめてください。
リストアップ結果は全部をまとめたテーブルにし、 button.md に保存してください。

モーダルの閉じるボタンがあります。
今はテキストになっているので、これをボイボ寮個別ページのホームボタンのようにアイコンに置き換えてください。
borderは不要です。

ボタンを統一してクラス化していきます。
ボタンなどの統一スタイルはcssにクラスとして定義し、cssをimportして必要なAstroやReactコンポーネントを作ってください。
cssはコンポーネントと同じところに置いてください。
tailwind v4での作り方にデファクトがあるので、必ずcontext7 mcpでtailwind関連について調べてください。
どのようなbuttonがあるか把握するために必ず button.md を読んでください。

まずはアイコンのみボタンのクラスを作ります。
おそらくモーダルの閉じるボタン・製品ページの前後移動ボタン・ボイボ寮ページのホームボタン・再生ボタンが該当するはずです。
ボーダーありバージョンとなしバージョン、あとサイズ違いに対応してください。
このボタンはボーダーありなし・サイズ違いの全バリエーションを想定してください。
色など、なるべく多くのことを共通化してください。微妙に違う等で共通化して良いかわからなかったら訪ねてください。

.tsxと.astroを作る場合、共通部分はなるべく切り出してください。共通関数はhelper.tsを作って共通化してください。
連動している引数は直和型にしてください。例えばborder: booleanとborderWidth: numberなどは直和型にするのが良いと思います。
不要な引数は追加しないでください。例えば全buttonがtype="button"であれば、type引数は無い方が良いです。

IconButtonに関して、色の共通化ができていません。
色を１つだけ指定すれば、border色や背景色を指定しなくても指定しなくても良い感じになるようにしてください。
どのようにすれば良さそうか提案してください。
微妙に違う差異は吸収する手もありそうです。どれを吸収するのが良さそうかも提案してください。
今回はborderがあるほうはOutline color variantsが、borderがない方はghostに近づけるのが良さそうです。

続いてIconButton以外のボタンについて共通化していきたいです。
rounded・iconありなしなどいろんなバリエーションのものがありますが、どれをどう共通化するのが良さそうか提案してください。

IconButtonとButtonに関する見た目展示用ファイルが dev/ui にあったと思います。
これを更新し、様々な見た目のものを表示できるようにしてください。
React版・Astro版両方ある場合はAstro版の方をいろんな種類載せ、React版は１種だけ載せてください。
片方しかない場合はその一方でいろんな種類載せてください。
スクリーンショットの更新はこちらで行うので禁止です。

モーダルを共通化してください。
モーダルは必ず動的なので、Reactコンポーネントだけ実装してください。
Reactだけなので`@utility`の実装は不要だと思います。

ボタン系（背景色がるaタグも含む）のhoverの挙動を統一してください。
background colorと文字色を一定の値で混ぜるようにしてください。
可能なら@variant hoverで統一化するのが良いですが、コンポーネント化されてないボタン系にも同様のスタイルを適用できるようにしたいです。

focus時になにかスタイルを変えている部分をすべてなくしてください。
ブラウザのデフォルトの挙動そのままになるようにしてください。

共通化したあと、コンポーネントの利用箇所でのコンポーネントの使い方・書式も統一していってください。
同じようなコンポーネントの実装に微妙な差異があるとき、`Preline UI`のDefaultテーマの同じコンポーネントのUIを参考にし、そちらに近いものを採用するようにしてください。
context7でPrelineのドキュメントを確認するのも参考になると思います。
細かい実装は`~/Github/preline`にあるので確認してください。

互換性を残したコードは複雑になるので禁止で、必ず既存コードを置き換える形で実装してください。
アニメーションは絶対必要でない限り禁止です。追加すべきな場合はユーザーに提案して許可を得てください。
フォールバックは禁止です。cssのvarの第二引数が不要なはずの場合は、書くことが禁止されています。
cssのスペース系の値指定は`--spacing(N)`を積極的に使ってください。

---

以上が今までのタスクです。
今回は以下のタスクを実行してください。

zag.jsは辞めて、Radix UIを導入していこうと思います。
zag.jsを使ってバニラJSでWeb Componentを作ることができなりますが、インストール数・非Reactなコンポーネントが少なそう・実装の手間を考えての判断です。
まずはzagjs系のライブラリを削除してください。
最新の情報を得るためにcontext7 mcpを積極的に使ってください。
まだコードは書き換えず、調査とリストアップだけ行ってください。

Radixを使ってDropdownを実装していきます。
まずはStyleDropdownをRadix化してください。
こちらが許可していれば破壊的変更をしても構わないので、より良くする提案があれば積極的に行ってください。
DropdownMenuで良さそうです。
hover開閉でも、クリックでの開閉でもどちらでも良いようにしたいです。
ただhoverしたあとクリックして閉じるのは防止したいです。
あとNミリ秒感だけeventを無視する処理は禁止です。

続いてNemoページのCVドロップダウンもRadix化してください。
その際、共通部分はhookにして切り出してください。

Modal系をRadix化してください。


astroやbulmaやtailwindやRadix UIについてわからないことがあればcontext7で調べてください。

---

最後に以下をすべて実行して通過させてください。
```bash
pnpm run fmt && pnpm run lint
pnpm run test-build && STRICT_USE_PREVIEW=1 pnpm run test:e2e
```
Codexは`pnpm run test-build`までで、`pnpm run test:e2e`は実行しないでください。

タスク遂行の際、余計なことをしないように注意してください。
テストが落ちた場合、設定変更で直さず、まず原因ログを提示して質問してから対応してください。
既存のコメントは消さないでください。
Codexへ：あなたは`playwright.config.mts`を書き換える悪癖があります。非常に注意して絶対に書き換えないように強く注意し、Planにもこのことを確実に記載してください。

Planの手法がうまくいかなかったら、**必ずAskSkillでどうするかユーザーに聞く**ように書いてください。
うまく行かない方法を何度も試されると迷惑なので。
